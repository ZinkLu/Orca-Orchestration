{
  "name": "orca-orchestration-studio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Orca DAG: a skill that teaches your agent to build an orchestration task DAG, plus a viewer binary to visualize it and fire tasks per-harness.",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"npm run dev -w server\" \"npm run dev -w web\"",
    "dev:server": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w web",
    "build:binary": "node scripts/build-binary.mjs",
    "build:npm": "node scripts/build-npm.mjs",
    "release": "node scripts/release.mjs",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "esbuild": "^0.25.0"
  }
}
